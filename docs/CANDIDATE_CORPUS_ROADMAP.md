# 候选语料库整理路线图

## 文档定位

本文记录统一候选语料库在完成来源合并、拼音门禁、轻声范围约束和运行库重建之后的工程现状，并规定
下一阶段的整理顺序。它是“现在整理到哪里、下一步整理什么”的长期入口。

这里使用“候选语料库”，而不简单称为“词库”，因为全集同时包含词、专名、术语、固定表达、能产
组合、句法片段、语料切片和少量确定性无效材料。系统不预先假定每个高频字串都是词，也不把低频、
古旧或暂时不能动态恢复等同于无效。

相关文档分工如下：

- [字词拼音与频次统一语料包](LEXICON_SOURCE_BUNDLE.md)：来源、构建口径和产物格式；
- [汉语输入候选整理与动态组合系统](INPUT_CANDIDATE_MODEL.md)：分类、决策覆盖层和动态组合器；
- 本文：现状快照、质量判断、整理优先级和分阶段路线；
- [当前实现总览](CURRENT_ARCHITECTURE.md)：整个拼音—音元—运行候选生产链的工程事实入口。

## 三层必须分开

```text
统一来源语料库 source_lexicon.sqlite3
  保存完整来源事实、读音、频次、分类和拒绝证据
              │
              ▼
候选整理覆盖层 input_model.sqlite3
  保存 proposed / approved / rejected / deferred 决策
              │
              ▼
运行候选 pinyin_hanzi.db
  只承载当前发布所需的静态候选和编码结果
```

来源语料库应尽量完整并可追溯；整理覆盖层可以逐步积累稀疏判断；运行候选以后可以缩减。不得为了
缩小运行词库而删除来源证据，也不得把尚未审查的机器建议直接写成生产事实。

## 2026-07-27 Windows 默认发布状态

“运行候选以后可以缩减”已经在 Yime for Windows 落地，不再只是路线设想：

- 两级核心发布词典包含 1,116,892 个不同字串、1,124,631 条已编码读音；
- 旧 2,456,797 条大词库退出安装运行链，但完整来源、phrase inventory 和候选决策均保留；
- 2,449 条固定回放的冷启动首选率为99.6325%，95% Wilson 下界99.3030%；
- 540,000 次加速用户学习漂移全部验收项通过；
- 纯净用户态人工压力测试验证5/5可构造、3/3一次纠正后首选且重启保持；
- 未预装专业短语“逼尿肌反射亢进”可由已编码组件动态生成并直接首选。

这回答的是发布层容量分工：统一来源库保留全部可追溯材料，默认运行层只接受过门禁组件。后续工作
从“大规模给所有字串注音或删低频项”转向动态覆盖残差、排序、真实缺词晋升、专业/专名硬失败和
用户库长期漂移监控。

## 2026-07-25 现状快照

下列数字来自最新本地统一包
`.generated/lexicon_source_bundle/source_lexicon.sqlite3` 及其验证报告。生成物不提交 Git，今后应以
重新构建后的 `manifest.json` 为准，不把本节数字当作永久锁定值。

### 来源与运行规模

| 项目 | 数量 | 含义 |
| --- | ---: | --- |
| 合规来源记录 | 2,663,036 | Unihan、pypinyin、万象通过来源门禁的原始证据行 |
| 有合规读音的不同字词 | 2,441,901 | 去重后的字串数 |
| 字词—读音输出记录 | 2,468,966 | 同一字串可以有多个合规读音 |
| 单字运行读音 | 60,985 | 46,088 个不同汉字的可运行单字读音 |
| 词语运行读音 | 2,395,813 | 进入当前静态运行链的多字读音 |
| 最终运行候选 | 2,456,798 | 当前 `runtime_candidates` 物化结果 |
| 来源拒绝记录 | 11 | 全部保留原值、来源、规则和原因 |

按来源计数：

| 来源 | 通过门禁的记录 |
| --- | ---: |
| Unihan | 56,208 |
| pypinyin 词语材料 | 411,958 |
| 万象 | 2,194,870 |

当前词语覆盖主要由万象提供，pypinyin 是重要旁证和补充，Unihan 负责单字读音底座。万象权重与
BCC 频次始终分列保存，不能互相替代。

### BCC 覆盖

| 项目 | 数量 |
| --- | ---: |
| BCC 不同字串 | 2,101,047 |
| 已匹配合规读音 | 433,971 |
| 尚无合规读音来源 | 1,667,076 |
| 未解码比例 | 约 79.3% |

未解码数量很大，不等于存在 166 万个真正的词尚未注音。BCC 字串中包含偶然相邻组合、句子截片、
数字和量词片段、公文套语片段、专名片段、古近代材料以及偶然切片。现阶段的核心任务是先判断组合
性质和动态可再现性，而不是给所有字串逐字猜读音。

BCC 单字已经基本覆盖：20,261 个不同单字中有 20,244 个具备合规读音，只余 17 个。真正的整理压力
已经从单字注音转向多字组合判断。

### 高频未解码分层

| BCC 汇总频次 | 未解码字串数 | 当前处理顺序 |
| --- | ---: | --- |
| ≥ 10,000 | 517 | 第一批人工与规则联合审查 |
| 1,000–9,999 | 11,275 | 第二批，适合形成首轮标注集 |
| 100–999 | 119,364 | 在规则和模型经过验证后批处理 |
| 10–99 | 937,162 | 主要依赖统计、上下文和动态恢复评测 |
| 1–9 | 598,758 | 最后处理，默认不因存在而进入静态候选 |

### 多读音与轻声

- 22,989 个不同字词存在多个合规读音，共形成 50,054 条冲突输出记录；
- 这些冲突可能是真多音、轻声、本调、文白异读、专名读音或来源错误，不能一律删成单音；
- 当前有 14 条读音被限制为 `word_context_only`，只保存词境证据，不生成孤立单字候选；
- `藍` 已验证这一机制：保留单字 `lan2`，词境 `lan5` 不生成单字候选，过时的 `la5` 被定点排除；
- 轻声由完整来源词音和来源标调制度约束，不再用早期“五声补齐”生成。

### 候选整理覆盖层

当前 `.generated/input_candidate_model/input_model.sqlite3` 已同步 4,108,988 个不同候选字串，覆盖
合规读音、BCC 字串和拒绝记录的字串并集。两字规则当前只形成动态可达性证据：

| 表 | 当前记录数 |
| --- | ---: |
| `candidate_universe` | 4,108,988 |
| `two_character_dynamic_reachability` | 735,923 |
| `recursive_composition_evidence` | 1,667,070 |
| `v_review_queue` | 4,108,988 |
| `assessments` | 0 |
| `context_evidence` | 0 |
| `composition_patterns` | 0 |

这 735,923 条均为整体没有合格词条读音、但左右两个单字分别已有合格注音的两字字串。该事实现在只
写为 `dynamic_reachable` 证据，不再自动归入 `productive_phrase + model_only + approved`，也不再
自动退出静态待编码系列。它们仍须由静态容量规划、规则族验证或人工 assessment 决定去留。
`assessments = 0` 表示尚无逐条人工覆盖。

2026-07-25 分层兜底递归组合模型进一步分析了全部 1,667,070 个未编码多字串：

- 1,667,070 项全部具有分层输入路径，可达率为 100%；
- 635,337 项的首选路径复用了至少一个已编码多字组件；
- 1,031,733 项当前只使用二至四字动态残余块，不再暴露顶层单字；
- 全库共使用 828,094 个二字块、682,995 个三字块和 14,900 个四字块，单字块为 0；
- 77,803 项存在多个等优覆盖方案，1,046,019 项因组件多音存在多个输入读音组合；
- 517 个 BCC 频次不低于 10,000 的诊断项中，112 项复用多字组件，405 项只用动态残余块，
  11 项结构多解，366 项读音多解，没有缺失单字根基例外。

这些结果只证明输入路径存在。结构多解和读音多解必须进入排序与回放；动态残余块也不能直接证明
目标不是固定词。组合序列不写回整体词音，候选状态仍保持原状。

已编码字串另由 `tools/plan_static_lexicon_capacity.py` 生成静态容量前沿：单字和没有更短同音节路径
的读音构成硬底座，其余字串只进入动态迁移候选；再用直接频次和组件复用价值安排不同容量的静态
保留次序。容量前沿是工程试验入口，不是删除清单，必须结合真实输入回放、候选排名、歧义和延迟
指标选定发布容量。

2026-07-25 首次全量规划结果如下：

| 容量层 | 字串数 | 直接 BCC 频次覆盖 |
| --- | ---: | ---: |
| 静态硬底座 | 52,290 | 74.9662% |
| 硬底座 + 10,000 个高效用项 | 62,290 | 91.6572% |
| 硬底座 + 50,000 个高效用项 | 102,290 | 97.4240% |
| 默认 98% 代理推荐 | 111,294 | 98.0000% |
| 硬底座 + 100,000 个高效用项 | 152,290 | 99.2252% |

本轮共分析 2,441,908 个已编码不同字串；硬底座含 46,095 个单字和 6,195 个至少有一个读音无法由
更短同音节条目覆盖的多字串。另有 2,389,618 个字串在读音层面可递归重建，其中默认推荐容量之外的
2,330,614 个只标为迁移候选，尚未从运行词库删除。

### 词库尾助词观察队列（2026-07-24）

`tools/export_lexicon_quality_review.py` 已能把只读 lint 信号与 `input_model.sqlite3` 合并，按本文相同的
BCC 频次档导出全量 TSV 和 Markdown 摘要。当前运行库得到 84,662 个不同待审字串，其中：

| BCC 汇总频次 | 待审字串数 |
| --- | ---: |
| ≥ 10,000 | 11 |
| 1,000–9,999 | 40 |
| 100–999 | 137 |
| 10–99 | 323 |
| 1–9 | 114 |
| 无 BCC 证据 | 84,037 |

这 84,662 项只是“末字和末音节读音符合助词/语气词观察规则”的信号，不是删除清单。高频样例同时包含
「为了」「除了」「喇嘛」等应保留材料和真正需要判断的句法片段，证明字形规则不能自动作发布决定。
当前 `context_evidence` 仍为 0；本地 BCC 输入只有分域频次表，无法从 count 反向重建 KWIC 原句。
因此近期先审 11 个最高频项，再审合计 51 个频次不低于 1,000 的项目；缺少上下文时允许 deferred。

2026-07-28 已完成首批 11 项：9 项明确保留，2 项因缺少上下文和动态回放保持 deferred，没有项目被
仅凭尾助词信号拒绝。重导出后待审队列为 84,653 项，已有决定而排除 9 项。裁决保存在
`internal_data/lexicon_review_decisions.json`。这批材料现已并入 R0–R5 全量覆盖闭环，完成证据见
[动态候选覆盖与核心提升闭环](DYNAMIC_CANDIDATE_COVERAGE.md)。

尾助词信号现升级为助词构式证据：1–4 字项先区分结构助词、动态／体貌助词、语气助词及多功能
助词，再分别标为有类型的组件角色；更长项标为 `dynamic_sentence_candidate`。分类同时记录
左右选择接口，进入核心的理由是构式生产力而不是“像不像词”。这仍只是审阅建议；固定词例外、
组件依赖、A/B 和动态回放决定最终发布去留。理论和门槛见
[助词构式系统与核心部件准入](PARTICLE_CONSTRUCTION_SYSTEM.md)。

纳入“地、得、着、过”并补齐语气助词后，真实运行库待审队列扩展为 99,909 个不同字串：
结构部件建议 21,697 项、体貌部件建议 6,793 项、体貌／语气多功能建议 38,507 项、语气部件
建议 28,394 项，另有 4,518 个长串动态恢复候选。该扩展是分类覆盖面的增加，不表示新增了
99,909 个应静态保留的词。

零证据长串另按运行容量而非语言价值整理：长度 ≥5、无 BCC、无下游复用、非强制静态且完全可
恢复的普通词汇／生产短语共 472,671 项，明确迁出静态核心；同条件下 71,574 个领域词和专名受
类别保护。当前两级运行选择保留 7,867 个有证据或受保护的二级长串，上述迁移对象误入数为 0。
规则及报告入口见 `tools/audit_long_form_core_migration.py`。

## 已经完成的整理能力

### 未编码字串可以进入可审计的人工准入流程

`tools/review_unencoded_candidates.py` 提供本机可视化工作台，面向没有合规读音、尚未进入已编码候选
的字串。审查者可以主要通过鼠标完成准入、拒绝或暂缓，并按材料分类和预期用途记录理由。所有判决
进入独立覆盖层并保留改判历史；即使人工准入，缺少真实读音来源的字串仍标记为不可进入运行词库，
不得由界面猜读音或生成编码。

对于能产短语和半固定构式，工作台已经提供独立的规则族登记：以一个代表例、同族正例、反例、结构
说明和排除边界为审查单位。已登记正例进入 `model_only` 并退出逐条待审队列，反例不被连带判决；
登记状态明确保持为“未验证、不可运行”。当前尚未实现的下一阶段是基于真实组件读音和回放指标把
通过验证的规则族提升为可执行组合规则，并批量标记 `dynamic_recoverable`。

构式发现已经支持正序前缀和逆序后缀的层级筛选。审查者可先用短根项取得全集，再用方向兼容的更长
项细分；系统按最长命中项拆分候选，并分别核验左右组件是否已有来源门禁合格读音。两侧合格的结果可
批量带入动态组合、半固定构式、专名或领域规则族的正例，缺读音项则留在来源核验路径；筛选本身不写
判决。

同一窗体也支持 `以{依据}为{目标}`、`在{处所}(中|上|下){后续?}` 一类框式模板。槽位整体有合规
读音时停止，否则从多字递归到已注音原子；两字组合在两个单字均有合格注音时直接进入动态组合，
只有至少一个单字缺音时进入专门例外，多种同等拆分进入歧义
审查。只有唯一递归覆盖的框式实例才能作为批量正例，模板及解析结构随规则族保存并由后端重新验证。

自动筛查层会把已登记规则应用到尚无 assessment 的未编码整体：唯一无冲突命中可批量写入
`model_only + unvalidated`，精确反例、缺音两字例外、拆分歧义、跨分类冲突和缺读音项目分别留队。
没有规则但可递归覆盖的项目标为 `unclassified_composition`，并按首字/末字聚类为后续规则建议。
因此人工工作量可以逐步从表面字串数量收敛到有限的新规则、冲突和例外数量，同时不放宽来源及运行时
门禁。

### 1. 来源边界已经闭合

- 只读取真实本地上游文件；
- BCC 各分域原始计数分别保存，汇总频次取各分域最大值；
- 不把二次合并频次文件重新当作来源；
- 万象的 `cuoyin`、`mixed` 默认不进入拼音解码来源；
- 每个输入文件在 manifest 中保存路径、摘要和大小。

### 2. 拼音进入编码前有统一门禁

- 非法字符、异常拼式和缺失注音可以拒绝并报告；
- 新音节必须有真实来源和明确审查，不自动补齐声调；
- 不猜读音，不手写音元码、Yinyuan ID 或键位；
- 过时读音可以定点排除，不扩张成全局音节禁用。

### 3. 轻声与词境读音已经分层

- 记录轻声位置和来源解释状态；
- 区分完整候选读音与仅供词境使用的证据；
- 单字本调、词汇性轻声和临时语流弱化不再压成同一张五声闭合表。

### 4. 来源真源能够传导到运行候选

统一来源库重建后，正式单字导入和运行刷新可以清除陈旧读音，而不仅是给旧行重算编码。来源排除、
单字库存、音节依据和运行候选已经形成可验证的闭环。

## 当前不能作出的结论

### “只拒绝 11 条”不等于词库已经很干净

来源门禁检查的是注音能否安全、可解释地进入编码链。一个字串有合法拼音，不代表它一定是词，也不
代表它值得静态进入候选框。万象中仍包含人名、地名、诗词、专业条目和联想组合；BCC 中仍包含大量
宽松切分结果。这些应在候选整理覆盖层判断，不应伪装成拼音错误。

### “BCC 高频”不等于“词汇性强”

高频只能决定审查优先级。某些高频项可能来自固定报头、日期模板、连续文本截片或历史语料格式。
必须结合来源分域、长度、上下文、左右边界和组合可恢复性判断。

### “可以动态组合”不等于“应立即删除静态词条”

动态组合必须先通过回放验证。只有当短组件已经批准、读音有来源、组合召回稳定且排序不恶化时，长
候选才可以标为 `dynamic_recoverable`。精简运行词库是路线末端，不是整理起点。

## 分阶段整理路线

### 阶段 A：固定可重复的质量快照（已完成）

目标：每次来源更新后都能回答“新增、减少和拒绝了什么”。

工作：

1. 重建统一来源包并执行验证；
2. 导出按 BCC 排序的未解码和多读音摘要；
3. 对比 manifest、拒绝数、冲突数和运行候选数；
4. 保证来源库、单字导入和运行刷新形成固定点。

完成标准：相同上游与策略连续重建得到相同统计和摘要；验证为零错误、零警告。

当前状态：已完成。来源包、输入模型、静态容量、运行选择和报告均有可重建 manifest。

### 阶段 B：递归筛查未编码字串并以 517 个最高频项作诊断集（已完成）

目标：先用较短已编码字串自动建立全量动态可达性证据，再用最高频项检验模型，而不是要求人工逐项
标注全部 517 项，也不急于为整体自动注音。

自动模型至少区分：

- 能由较短已编码组件唯一恢复的组合；
- 存在多个等优切分的结构多解；
- 组件存在多个来源读音的读音多解；
- 因尚无已编码单字根基而无法覆盖的材料；
- 虽技术可达、但仍需上下文判断是否固定词或专名的材料。

完成标准：全量未编码多字串均具有可复现的递归组合证据或明确缺口；517 个高频项只作为诊断和抽样
复核集，人工集中处理模型冲突、来源缺口和规则无法解释的残余项。组合输入序列不得写成整体词音。

当前状态：已完成。1,667,070 个未编码多字串全部获得分层路径，最高频 517 项作为诊断集保留。

### 阶段 C：建立上下文证据和保守规则（构式基础设施已完成）

目标：把“像不像词”的直觉转为可复查证据。

优先增加：

1. BCC KWIC 前后文；
2. 左右邻接频次、左右熵和互信息；
3. 数字、时间、数量结构和标点边界规则；
4. 助词、语气词、连词、量词及框式结构的切分建议；
5. 万象显式类别到人名、地名、术语等分类的保守映射。

规则只能输出 `proposed` 建议。不能因为命中规则就自动批准，更不能由 LLM 填写拼音或编码。

完成标准：第二批 11,275 个高频项能够按可解释特征分队列，并形成可抽样评估的建议集。

当前状态：助词构式、框式模板、前后缀发现、规则族和审查队列已程序化。KWIC 等新证据可持续补充，
但不再阻塞原 Phase 4.3 的关闭。

### 阶段 D：整理多读音冲突（处理机制已完成）

目标：把“同形多读”拆成默认读音、限定读音和来源错误，而不是粗暴去重。

审查顺序：

1. 运行高频且存在 BCC 证据的冲突；
2. 单字本调与词内轻声冲突；
3. 人名、地名、姓氏与普通词读音冲突；
4. 文白、古今和方言读音；
5. 低权重孤立来源与可能的注音错误。

完成标准：高频冲突具有明确默认读音或适用范围；无法判断者保持多读或 `deferred`，不猜选。

当前状态：多读音保留、词境限定、来源冲突和 `deferred` 路径已落地；新增来源继续走常规质量门禁。

### 阶段 E：动态组合回放（首轮验收已完成）

目标：验证哪些长候选可以由来源门禁合格的短组件恢复，以及哪些自动可达路径能够安全进入发布
运行规则。证据阶段不要求短组件先作人工分类；发布提升仍须回放。

评测至少包括：

- 目标候选召回率；
- 首屏和前若干位排序；
- 多音组件带来的组合歧义；
- 静态保留与动态恢复的内存、速度和候选质量差异；
- 人名、机构名、框式结构和数量结构等不同类别的分项表现。

完成标准：只有通过回放阈值的条目才能标记为 `dynamic_recoverable`。

当前状态：固定回放 5,830 项；生产范围 2,449 项冷启动首选率99.6325%，95% Wilson 下界99.3030%。

### 阶段 F：形成精简发布层（已完成）

目标：在不破坏来源事实和召回率的前提下缩小静态运行候选。

发布政策：

- 高频基础词、固定表达、重要专名和不能稳定动态恢复的材料静态保留；
- 已验证可恢复的长组合转入动态层；
- 明确无效材料进入 R0 修复或隔离，不用频率替代判定；
- 领域词库可以按场景分包，不必全部常驻；
- 完整来源证据和整理决策继续保留，不随运行精简删除。

完成标准：精简前后具有可比较的回放报告，并能从同一来源真源稳定再生成。

当前状态：Windows 两级运行层已落地；1,116,892 个不同字串被选择，472,671 个可恢复零证据长串
迁出静态核心，迁移违规为 0。

## 每轮整理的固定工作流

```powershell
# 1. 重建统一来源语料包
.\venv312\Scripts\python.exe tools\build_lexicon_source_bundle.py

# 2. 校验来源库
.\venv312\Scripts\python.exe internal_data\pinyin_source_db\validate_source_pinyin_db.py

# 3. 更新可提交的高频审查摘要
.\venv312\Scripts\python.exe tools\export_lexicon_review_summary.py

# 4. 同步完整候选整理全集
.\venv312\Scripts\python.exe tools\build_input_candidate_model.py

# 5. 用较短已编码串递归筛查未编码长串
.\venv312\Scripts\python.exe tools\build_recursive_composition_model.py

# 6. 对全部已编码候选执行 R0-R5 动态覆盖验收
.\venv312\Scripts\python.exe tools\evaluate_dynamic_candidate_coverage.py
```

来源或单字读音发生变化并需要进入运行库时，还应走正式 prototype 单字导入和运行刷新链；不得直接
删除 SQLite 中的个别运行行。

## 持续优化（不再作为 Phase 4.3 未完成项）

无 BCC 条目的同频长尾也不再挂作未完成清理：运行排序已经实现 BCC 主证据、
RIME-LMDG 缺失补充、静态效用非频次保底的严格分层。详见
[候选排序证据与长尾结构](CANDIDATE_RANKING_EVIDENCE.md)。新增独立语料只替换临时证据，
不会反写 BCC，也不会重新打开删除式“噪声清理”。

2026-07-28 全量报告已经为 2,441,908 个已编码字串逐一分层：R0 0、R1 4,798、R2 1,397、
R3 60,312、R4 2,054,255、R5 321,146，完成门禁通过。R1–R3 是动态核心的持续改进队列，不是
删除队列，也不使本项重新变为“待做”。

后续迭代按收益处理：

1. 为 R1 找最小可复用部件或构式；
2. 为 R2 补齐缺失读音覆盖；
3. 为 R3 评估中间部件、搜索宽度或有界缓存并做排序回放；
4. 持续验证 R4 的动态服务质量和 R5 的静态保留理由。

1. 保持纯净 Windows 用户库连续积累开发文本，周期性记录冷启动纠正、重复首选、重启保持和用户库
   增长；
2. 对 `lexicon-promotion-scan` 发现的高频缺词做跨周期审核，避免把单人误选直接提升为系统词；
3. 扩大医学、生物、化学、姓名、地名、古籍及扩展区汉字的硬失败样本，优先检查“根字符/读音缺失”
   而不是完整长词是否预装；
4. 固定旧大词库安装泄漏、反查核心优先级和全新用户目录部署回归；
5. 继续保存早期容量档作离线对照，但不再把它们描述为当前 Windows 默认方案；
6. 获得真实用户遥测后，再校准产前99%估计和公共词条晋升阈值。

候选整理工作的最终交付不再是覆盖全部来源字串的人工分类表，而是分层输入架构的训练、回放和容量
证据：来源全集用于基础解码门禁和测试；可恢复材料进入动态组合回放；用户真实选择进入个人学习；
上下文转移先用本地数据模拟语义排序，将来可由远程排序服务替换。具体边界见
[分层输入架构迁移](LAYERED_INPUT_ARCHITECTURE.md)。

路线的核心不是给四百多万字串逐一贴上“词/非词”二元标签，而是建立一套能够保存不确定性、区分
来源事实与发布决策、并用动态恢复逐步替代冗余静态组合的输入模型。
