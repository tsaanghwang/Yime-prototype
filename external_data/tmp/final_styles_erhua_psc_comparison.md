# PSC 儿化数据派生结果与外部简表对照

> **来源边界：本报告中的项目结果先由已转录 PSC 数据独立生成。外部 Excel 只在生成完成后用于核对数量和形式，不导入、不参与分类，也不反向改写项目数据。**
> 对照日期：2026-08-16  
> 事后核对材料：`C:\dev\PSC-Outline\普通话儿化音.xlsx`，工作表“韵母规则简表”  
> 项目材料：`external_data/tmp/final_styles_erhua_draft.json`  
> 范围：离线研究草稿；不接运行时。

## 数量

| 项目 | 工作表 | 项目草稿 | 结论 |
|---|---:|---:|---|
| 原韵母—儿化结果规则 | 37 | 37 | 一致 |
| 儿化结果类别 | 26 | 26 | 一致 |
| 鼻化结果类别 | 8 | 8 | 一致 |
| 舌尖元音记录 | 2 | 2 | 一致：`i[ɿ]`、`i[ʅ]` 分列 |

## 形式

- 单韵母 `e[ɤ]` 单列为 `er[ɤr]`，不再与一般 `er` 类混在同一 UI 结果类中。
- 鼻化类别不再显示“（鼻化）”后缀，而把组合鼻化符号直接加在 `r` 前的音质字符上：
  `ãr`、`iãr`、`uãr`、`ẽr`、`uẽr`、`i:ẽr`、`õr`、`iõr`。
- 项目 PSC 原始转录中 `o` 的来源基式保留为 `(o)`，工作表写作 `o`。这是来源记号层的唯一字面差异；
  UI 和规则比较均使用规范韵母 `o`，对应结果同为 `or`。
- 除上述保留的原始记号差异外，37 个规范化“原韵母→儿化结果”对逐项一致。

## 数据边界

项目先读取 `final_styles_erhua_draft.json` 中既有的 PSC `source_rule`、`source_base_final`、
`source_erhua_final`、`nasalized` 和对齐信息，自动生成 UI 分类；随后才把生成结果与外部表比较。
本次只调整派生分类、UI 记录和离线对照报告。`source_rule`、`source_base_final`、
`source_erhua_final` 等 PSC 原始转录字段保持不变；鼻化显示、`er[ɤr]` 说明和舌尖前后分列均由
派生层生成。
