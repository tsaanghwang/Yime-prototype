[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yime"
version = "0.1.1"
description = "Windows IME prototype and supporting pinyin rebuild pipeline for Yime."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"colorama>=0.4.6",
	"pynput>=1.7.6",
	"pywin32>=305; platform_system == 'Windows'",
	"tqdm>=4.65.0",
]

[project.optional-dependencies]
dev = [
	"coverage>=7.0.0",
	"pytest>=8.0.0",
	"pytest-cov>=3.0.0",
]
portable = [
	"pyinstaller>=6.12.0",
]

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["yime*", "syllable*", "utils*"]
exclude = ["tests*", "tools*", "yime.legacy*"]

[tool.setuptools.package-data]
"syllable.analysis" = ["yinyuan/*.json"]
"syllable.codec" = ["*.json"]
"syllable.pianyin" = ["*.json"]
"yime" = ["*.json", "*.sql", "*.txt", "*.csv"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["tests/manual"]
python_files = ["test_*.py", "*_test.py", "verify_*.py"]
