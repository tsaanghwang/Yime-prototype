from setuptools import setup, find_packages

setup(
    name="finals_classifier",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 无额外运行时依赖
    ],
    python_requires=">=3.8",
)